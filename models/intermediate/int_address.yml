version: 2

models:
  - name: int_address
    description: "Intermediate address data"
    columns:
      - name: addressid
        description: "Address id"

      - name: address
        description: "Address"

      - name: address_type
        description: "Address type"

      - name: postalcode  
        description: "Postal code"

      - name: city
        description: "City"

      - name: state
        description: "State province id"

      - name: country
        description: "Country region code"
